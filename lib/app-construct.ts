import {Construct} from "constructs";

interface AppConstructProps {

}


export class AppConstruct extends Construct {
  constructor(scope: Construct, id: string, props: AppConstructProps) {
    super(scope, id);


  }
}

